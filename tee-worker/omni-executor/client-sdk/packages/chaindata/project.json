{
  "name": "chaindata",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/chaindata/src",
  "projectType": "library",
  "targets": {
    "build": {
      "outputs": ["{options.outDir}"],
      "executor": "nx:run-commands",
      "options": {
        "cwd": "./packages/chaindata",
        "command": "npx tsup",
        "assets": ["packages/chaindata/*.md"],
        "outDir": "dist"
      }
    },
    "publish": {
      "command": "node tools/scripts/publish.mjs chaindata {args.tag}",
      "dependsOn": ["build"]
    },
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "packages/chaindata/**/*.ts",
          "packages/chaindata/package.json"
        ]
      }
    }
  },
  "tags": []
}

      